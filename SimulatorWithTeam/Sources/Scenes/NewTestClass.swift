//
//  NewTestClass.swift
//  SimulatorWithTeam
//
//  Created by Роман Глухарев on 22.03.24.
//

import Foundation
import UIKit

class TestTestTestClass: UIViewController {
    
    private lazy var someButton: UIButton = {
        let button = UIButton(type: .system)
        return button
    }()
    
    private lazy var label: UILabel = {
        let label = UILabel()
        return label
    }()
    
    private lazy var textView: UITextView = {
        let text = UITextView()
        return text
    }()
    
    private lazy var someeButton: UIButton = {
        let button = UIButton(type: .system)
        return button
    }()
    
    private lazy var texttView: UITextView = {
        let text = UITextView()
        return text
    }()
    
    private lazy var textwView: UITextView = {
        let text = UITextView()
        return text
    }()
    
    private lazy var textweView: UITextView = {
        let text = UITextView()
        return text
    }()
    
    private lazy var somewweButton: UIButton = {
        let button = UIButton(type: .system)
        return button
    }()
    
    private lazy var somewwwButton: UIButton = {
        let button = UIButton(type: .system)
        return button
    }()
    
    private lazy var somewwButton: UIButton = {
        let button = UIButton(type: .system)
        return button
    }()
    
    private lazy var somewwwwwButton: UIButton = {
        let button = UIButton(type: .system)
        return button
    }()
    
    
    
}

